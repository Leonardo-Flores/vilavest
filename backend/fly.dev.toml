# ============================================================
# Fly.io config — VilaVest Backend (DEV)
# Deploy: flyctl deploy --config fly.dev.toml --remote-only
# ============================================================

app            = "vilavest-api-dev"
primary_region = "gru"            # São Paulo (closest to Brazil)
kill_signal    = "SIGTERM"
kill_timeout   = "30s"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT            = "8080"
  SERVER_HOST     = "0.0.0.0"
  APP_ENV         = "development"
  LOG_LEVEL       = "debug"
  DB_SSLMODE      = "require"
  FRONTEND_URL    = "https://dev.vilavest.leonardoflores.dev.br"
  ALLOWED_ORIGINS = "https://dev.vilavest.leonardoflores.dev.br,http://localhost:5173"
  # Secrets set via: flyctl secrets set DATABASE_URL=... JWT_SECRET=...

[http_service]
  internal_port       = 8080
  force_https         = true
  auto_stop_machines  = "stop"   # Stop when idle to stay in free tier
  auto_start_machines = true
  min_machines_running = 0
  processes           = ["app"]

  [[http_service.checks]]
    interval     = "30s"
    timeout      = "5s"
    grace_period = "10s"
    method       = "GET"
    path         = "/health"

[[vm]]
  cpu_kind = "shared"
  cpus     = 1
  memory   = "256mb"
